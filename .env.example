# Exemplo de configuração de variáveis de ambiente
# Copie este arquivo para .env e preencha com seus valores reais

# Configuração do servidor
NODE_ENV=development
PORT=5000

# Segurança
SESSION_SECRET=chave-secreta-muito-segura-e-longa

# Banco de dados
# A URL abaixo é apenas um exemplo, substitua com a URL fornecida pelo Railway ou seu provedor PostgreSQL
DATABASE_URL=postgresql://user:password@hostname:port/database?sslmode=require